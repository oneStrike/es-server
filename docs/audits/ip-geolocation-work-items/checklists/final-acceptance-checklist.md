# IP 属地接入最终验收清单

## 功能验收

- [ ] `6` 张目标表已补齐统一属地字段。
  - 证据：
- [ ] Geo 平台能力可稳定输出 `geoCountry/geoProvince/geoCity/geoIsp/geoSource`。
  - 证据：
- [ ] 管理端可上传合法 `ip2region_v4.xdb` 并让当前进程立即切换到新库。
  - 证据：
- [ ] 管理端 `ip2region` 状态接口可返回当前生效文件与生效时间。
  - 证据：
- [ ] app / admin 登录态持久化记录包含属地字段。
  - 证据：
- [ ] 后台请求审计日志记录包含属地字段。
  - 证据：
- [ ] 论坛主题创建记录包含属地字段。
  - 证据：
- [ ] 评论创建 / 回复记录包含属地字段。
  - 证据：
- [ ] 论坛操作日志记录包含属地字段。
  - 证据：

## 契约与回归验收

- [ ] app 端论坛主题 `page`、`detail`、`my/page` 返回归属地字段。
  - 证据：
- [ ] app 端评论 `my/page`、`reply/page` 以及复用目标评论列表能力的现有接口（论坛主题、作品、章节）返回归属地字段。
  - 证据：
- [ ] 后台审计日志页面未新增属地筛选条件。
  - 证据：
- [ ] 论坛操作日志页面未新增属地筛选条件。
  - 证据：
- [ ] 后台若被动返回只读属地字段，未引入基于属地的查询、排序或业务判断。
  - 证据：
- [ ] 非法或损坏的 `xdb` 上传不会影响当前在线查询能力。
  - 证据：
- [ ] 关键写入链路与 app 返回契约的自动化测试已补齐或更新。
  - 证据：
- [ ] 既有分页、排序、鉴权和错误语义未发生回归。
  - 证据：

## 数据与迁移验收

- [ ] `pnpm db:generate` 已完成，迁移文件由规范流程生成。
  - 证据：
- [ ] `db/comments/generated.sql` 已同步更新，`pnpm db:comments:check` 通过。
  - 证据：
- [ ] `geoSource` 在所有落库样例中固定为 `ip2region`。
  - 证据：
- [ ] 属地字段为空时的语义已验证为“未能解析或未提供 IP”，不存在空字符串污染。
  - 证据：
- [ ] 已确认不兼容历史数据：不回填旧记录、不在读路径补算属地，旧记录返回空值符合预期。
  - 证据：
- [ ] 管理端上传后服务重启，仍优先加载 `active` 目录中的最后一次生效文件。
  - 证据：

## 验证命令与输出

- [ ] `pnpm type-check`
  - 输出：
- [ ] `pnpm db:comments:check`
  - 输出：
- [ ] 变更文件 `eslint`
  - 输出：
- [ ] 关键接口或服务验证
  - 输出：

## 阻塞上线项

- [ ] 无阻塞上线项
- 阻塞项记录：
  - 暂无

## 最终签收问题与结论

- 签收问题：
  - 暂无
- 最终结论：
  - 待验收
