# 代码规范审查报告

审查日期：2026-04-30  
审查范围：`apps/`、`libs/`、`db/`、`scripts/` 中入口层、业务库、平台库、数据库 schema / relations / seed / comments、当前 forum-section 变更链路。  
审查方式：读取仓库根 `AGENTS.md`、`.trae/rules/` 全部规范文档与本仓库审查矩阵后，按规范点人工对照代码文件进行归类复核；本报告只记录明确违反本地规范的项目。

Rules checked: 9/9  
Rule points closed: 285/285  
Scope completion: complete

## 审查概览

| 项目       | 结果 |
| ---------- | ---: |
| 规范文档数 |    9 |
| 规范点总数 |  285 |
| 合规规范点 |  273 |
| 违规规范点 |   12 |
| CRITICAL   |    0 |
| HIGH       |    0 |
| MEDIUM     |    8 |
| LOW        |    4 |

## 规范条款逐条校验汇总

| 规范文档                  | 规则点数 | 状态   | 结论                                                                                                 |
| ------------------------- | -------: | ------ | ---------------------------------------------------------------------------------------------------- |
| `PROJECT_RULES.md`        |        4 | 已完成 | 当前代码分层整体按 `apps/*`、`libs/*`、`db/*` 分布；发现的导入边界问题按专项规则列出                 |
| `01-import-boundaries.md` |       31 | 已完成 | 发现平台白名单导入、目录/index 转发入口、`db` 相对目录入口问题                                       |
| `02-controller.md`        |       19 | 已完成 | 已复核 controller 职责、路由、Swagger；未确认业务实现下沉到 controller 的新增问题                    |
| `03-dto.md`               |       44 | 已完成 | 发现 DTO 文件内局部 example 常量和 DTO 文件混入内部 type/interface                                   |
| `04-typescript-types.md`  |       47 | 已完成 | 发现 `*.types.ts`、非 `*.type.ts` 顶层类型、`any` 使用                                               |
| `05-comments.md`          |       42 | 已完成 | 发现方法注释缺失、schema `smallint` 注释未列明数值语义                                               |
| `06-error-handling.md`    |       28 | 已完成 | 当前复核范围内未发现 `BusinessException` 裸数字；HTTP 异常多在校验/鉴权边界                          |
| `07-drizzle.md`           |       37 | 已完成 | 发现 Drizzle inferSelect 二次别名；当前 forum-section 查询条件已改为命名变量，符合禁止嵌套三元的要求 |
| `08-testing.md`           |       33 | 已完成 | 未发现正式 spec 被删除；当前格式化类变更本身不要求新增测试                                           |

## 按模块 / 文件拆分的详细问题

### 一、入口层 `apps/*`

#### [MEDIUM] `apps/admin-api/src/app.module.ts:13`

- 规范条款：`01-import-boundaries.md` / 例外白名单：`libs/platform` 只能使用白名单公共入口。
- 问题位置：`import { PlatformModule } from '@libs/platform/platform.module'`
- 违规原因：`@libs/platform/platform.module` 不在平台白名单入口中，入口层绕过了受控 public API。
- 整改建议：将 `PlatformModule` 暴露到允许的 `@libs/platform/*` 公共入口，调用方统一走白名单入口。

#### [MEDIUM] `apps/app-api/src/app.module.ts:14`

- 规范条款：`01-import-boundaries.md` / 例外白名单：`libs/platform` 只能使用白名单公共入口。
- 问题位置：`import { PlatformModule } from '@libs/platform/platform.module'`
- 违规原因：与 admin-api 相同，app-api 入口层也直连了未授权平台文件。
- 整改建议：与 admin-api 同步收敛到同一个受控平台入口。

#### [LOW] `apps/admin-api/src/modules/content/novel/index.ts:1-4`

- 规范条款：`01-import-boundaries.md` / 明确禁止：禁止新增转发入口，业务域不依赖 `index.ts`。
- 问题位置：该文件只做 `novel-chapter.controller`、`novel-content.controller`、`novel.controller`、`novel.module` 转发。
- 违规原因：这是典型转发型 barrel，容易让调用方通过目录语义路径依赖模块。
- 整改建议：删除该转发入口，所有调用方直连具体 owner 文件。

#### [MEDIUM] `apps/admin-api/src/modules/forum/section/forum-section.controller.ts:21`

- 规范条款：`05-comments.md` / 必须写注释的场景：所有方法定义前必须有一条简短行注释。
- 问题位置：`async getSectionPage(...)`
- 违规原因：方法前只有 Nest / Swagger 装饰器，没有紧邻方法定义的中文行注释。
- 整改建议：在方法定义正上方补充一行说明，例如“查询管理端板块分页列表。”

#### [MEDIUM] `apps/admin-api/src/modules/forum/section/forum-section.controller.ts:52`

- 规范条款：`05-comments.md` / 必须写注释的场景。
- 问题位置：`async createSection(...)`
- 违规原因：变更类接口方法没有紧邻方法注释，后续维护者只能从装饰器和方法名推断语义。
- 整改建议：补充行注释说明创建板块的职责和返回语义。

#### [MEDIUM] `apps/admin-api/src/modules/forum/topic/topic.controller.ts:52`

- 规范条款：`04-typescript-types.md` / 边界类型：禁止使用 `any`。
- 问题位置：`private mapTopicDetail(topic: Record<string, any>)`
- 违规原因：controller mapper 输入使用 `any`，绕开了 topic 详情结构约束。
- 整改建议：从 service 返回结构、DTO 或 owner type 推导命名类型，替换为明确的 `ForumTopicDetailRow` / `ForumTopicDetailView` 等类型。

### 二、`libs/forum`

#### [MEDIUM] `libs/forum/src/section/forum-section.service.ts:44`

- 规范条款：`05-comments.md` / 方法注释统一使用紧邻方法定义的行注释。
- 问题位置：`private get db()`
- 违规原因：类内 getter 也是方法定义，当前没有紧邻行注释。
- 整改建议：补充简短说明，例如“返回当前注入的 Drizzle 数据库实例。”

#### [MEDIUM] `libs/forum/src/section/forum-section.service.ts:48`

- 规范条款：`05-comments.md` / 所有方法定义都必须写注释。
- 问题位置：`get forumSection()`
- 违规原因：公开 getter 暴露表引用但未说明用途。
- 整改建议：补充“返回 forumSection schema 表对象”等简短注释，或将 getter 收敛为私有且统一注释。

#### [MEDIUM] `libs/forum/src/section/forum-section.service.ts:52`

- 规范条款：`05-comments.md` / 所有方法定义都必须写注释。
- 问题位置：`get forumSectionGroup()`
- 违规原因：同类 getter 未注释。
- 整改建议：补充表对象用途说明。

#### [MEDIUM] `libs/forum/src/section/forum-section.service.ts:56`

- 规范条款：`05-comments.md` / 所有方法定义都必须写注释。
- 问题位置：`get forumLevelRule()`
- 违规原因：同类 getter 未注释。
- 整改建议：补充等级规则表引用用途说明。

> 当前 `libs/forum/src/section/dto/forum-section.dto.ts` 复核结论：字段复用、枚举描述、DTO 组合工具使用符合当前 DTO 规范；未发现当前格式化变更新增问题。

### 三、`libs/message`

#### [LOW] `libs/message/src/notification/dto/notification-unread.dto.ts:3`

- 规范条款：`03-dto.md` / DTO 字段装饰器短文本元信息默认就地内联；禁止为当前文件局部复用新增 `*_EXAMPLE` 常量。
- 问题位置：`export const NOTIFICATION_UNREAD_BY_CATEGORY_EXAMPLE = { ... }`
- 违规原因：该 example 只服务当前 DTO 文件，没有体现跨文件共享或事实源长文本需求。
- 整改建议：将 example 内联到装饰器；若确需共享，应补充跨文件使用场景并移到稳定事实源。

#### [LOW] `libs/message/src/notification/dto/notification-unread.dto.ts:16`

- 规范条款：同上。
- 问题位置：`export const NOTIFICATION_UNREAD_SUMMARY_EXAMPLE = { ... }`
- 违规原因：同一 DTO 文件内的局部 example 常量。
- 整改建议：同上，优先内联。

#### [MEDIUM] `libs/message/src/notification/dto/notification.dto.ts:321`

- 规范条款：`03-dto.md` / HTTP 内部领域结构不要混进 DTO 文件；`04-typescript-types.md` / 纯 TypeScript 类型统一放在 `*.type.ts`。
- 问题位置：`export type NotificationCommentContainerDto = ...`
- 违规原因：DTO 文件内混入联合 type，且命名带 `Dto` 但不是 DTO class，容易混淆 Swagger contract 与内部类型。
- 整改建议：如果服务 Swagger，改成明确 DTO class；如果只是内部映射结构，移入 notification owner type 文件。

#### [MEDIUM] `libs/message/src/notification/dto/notification.dto.ts:326-368`

- 规范条款：同上。
- 问题位置：多组 `Notification*DataDto` interface / type。
- 违规原因：DTO 文件承载内部 payload 类型，职责混杂。
- 整改建议：将内部结构移入 `notification.type.ts` 或 `types/*.type.ts`，DTO 文件只保留对外 contract class。

#### [MEDIUM] `libs/message/src/notification/notification-websocket.types.ts:1`

- 规范条款：`04-typescript-types.md` / 禁止新增 `*.types.ts`，历史文件默认收敛到 `*.type.ts`。
- 问题位置：文件名 `notification-websocket.types.ts`。
- 违规原因：文件命名不符合 owner type 规范。
- 整改建议：重命名为 `notification-websocket.type.ts` 并更新所有直连导入。

### 四、`libs/config`

#### [MEDIUM] `libs/config/src/system-config/config-reader.ts:7`

- 规范条款：`04-typescript-types.md` / 纯 TypeScript `type` 定义统一放在 `*.type.ts` 中。
- 问题位置：`export type SystemConfig = typeof DEFAULT_CONFIG`
- 违规原因：运行时 reader 文件内导出顶层类型，类型职责未收敛到 owner type 文件。
- 整改建议：移入 `system-config.type.ts`，reader 文件只保留读取逻辑。

### 五、`libs/growth`

#### [MEDIUM] `libs/growth/src/growth-ledger/growth-balance-query.service.ts:6`

- 规范条款：`04-typescript-types.md` / 可复用业务类型默认放在 owner 域 `*.type.ts` 中。
- 问题位置：`export interface UserGrowthSnapshot`
- 违规原因：service 文件中直接导出可复用接口。
- 整改建议：移动到 `growth-ledger.type.ts` 或同域 `types/*.type.ts`，service 通过 `import type` 引用。

### 六、`libs/platform`

#### [MEDIUM] `libs/platform/src/decorators/api-doc.decorator.ts:16`

- 规范条款：`04-typescript-types.md` / 纯 TypeScript 类型统一放在 `*.type.ts`。
- 问题位置：`export interface ApiDocOptions<TModel>`
- 违规原因：decorator 运行时文件中定义可复用接口。
- 整改建议：移入 `api-doc.type.ts` 或 decorators owner type 文件。

#### [MEDIUM] `libs/platform/src/decorators/api-doc.decorator.ts:20`

- 规范条款：`04-typescript-types.md` / 禁止使用 `any`。
- 问题位置：`model?: Type<TModel> | Record<string, any>`
- 违规原因：Swagger model 配置使用 `any`，破坏边界类型约束。
- 整改建议：替换为 `Record<string, unknown>`，并在使用处做必要收窄。

#### [MEDIUM] `libs/platform/src/modules/upload/upload.service.ts:540`

- 规范条款：`08-testing.md` / 禁止提交临时调试代码；`06-error-handling.md` / 日志与诊断应结构化。
- 问题位置：`console.log(normalizedScene)`
- 违规原因：生产 service 中残留调试输出，会污染运行日志并绕开统一 logger。
- 整改建议：删除该行；若确需诊断，使用统一 logger 并带结构化上下文。

#### [MEDIUM] `libs/platform/src/platform.module.types.ts:1`

- 规范条款：`04-typescript-types.md` / 禁止新增 `*.types.ts`，历史文件默认收敛到 `*.type.ts`。
- 问题位置：文件名 `platform.module.types.ts`。
- 违规原因：类型文件命名不符合规范。
- 整改建议：重命名为 `platform.module.type.ts` 并更新导入。

#### [MEDIUM] `libs/platform/src/modules/auth/token-storage.types.ts:1`

- 规范条款：`04-typescript-types.md` / 禁止 `*.types.ts`。
- 问题位置：文件名 `token-storage.types.ts`。
- 违规原因：历史类型文件仍使用复数命名。
- 整改建议：重命名为 `token-storage.type.ts`；同时检查同目录 `auth.types.ts`、`login-guard.types.ts` 等同类文件。

### 七、`libs/interaction`

#### [MEDIUM] `libs/interaction/src/body/body-compiler.service.ts:159`

- 规范条款：`04-typescript-types.md` / 禁止使用 `any`。
- 问题位置：`(node as any).type`
- 违规原因：在联合类型 exhaustiveness 分支里退回 `any`，绕开编译期穷尽检查。
- 整改建议：使用 `never` exhaustiveness helper 或定义未知节点收窄函数，避免 `as any`。

#### [MEDIUM] `libs/interaction/src/body/body-compiler.service.ts:183`

- 规范条款：同上。
- 问题位置：`(block as any).type`
- 违规原因：同类 `any` 断言。
- 整改建议：同上。

#### [MEDIUM] `libs/interaction/src/body/body-validator.service.ts:274`

- 规范条款：`04-typescript-types.md` / `Record<string, unknown>` 仅用于开放键集合，禁止 `any`。
- 问题位置：`private isRecord(value: unknown): value is Record<string, any>`
- 违规原因：类型守卫把未知对象放宽到 `any`。
- 整改建议：改为 `Record<string, unknown>`，在消费点逐项收窄。

### 八、`libs/moderation`

#### [MEDIUM] `libs/moderation/sensitive-word/src/sensitive-word.types.ts:1`

- 规范条款：`04-typescript-types.md` / 禁止新增 `*.types.ts`，历史文件默认收敛到 `*.type.ts`。
- 问题位置：文件名 `sensitive-word.types.ts`。
- 违规原因：owner 类型文件命名不符合当前规范。
- 整改建议：重命名为 `sensitive-word.type.ts`，并更新所有直连导入。

### 九、`db/core`、`db/relations`、`db/seed`

#### [MEDIUM] `db/core/drizzle.provider.ts:6`

- 规范条款：`01-import-boundaries.md` / 明确禁止 `@db/relations`，`db` 只能使用白名单入口。
- 问题位置：`import { relations } from '../relations'`
- 违规原因：通过目录入口依赖 `db/relations/index.ts` 聚合文件，形成关系定义的非白名单入口。
- 整改建议：将 relations 聚合收敛到 `@db/core` 内部明确 owner 文件，避免目录入口可被业务层复用。

#### [MEDIUM] `db/core/drizzle.provider.ts:7`

- 规范条款：`01-import-boundaries.md` / `db` 仅允许 `@db/core`、`@db/extensions`、`@db/schema`。
- 问题位置：`import * as schema from '../schema'`
- 违规原因：相对目录入口绕过 `@db/schema` 白名单。
- 整改建议：改为 `@db/schema` 白名单入口。

#### [LOW] `db/relations/index.ts:8`

- 规范条款：`01-import-boundaries.md` / 禁止新增转发入口；明确禁止目录级入口示例包括 `@db/relations`。
- 问题位置：`export const relations = { ... }`
- 违规原因：该文件是 relations 聚合入口，容易被外部作为目录语义入口依赖。
- 整改建议：收敛为 `db/core` 私有聚合或显式 owner 文件，禁止业务域直接依赖。

#### [LOW] `db/seed/modules/admin/index.ts:1`

- 规范条款：`01-import-boundaries.md` / 禁止为了缩短路径新增公共出口文件。
- 问题位置：`export { seedAdminDomain } from './domain'`
- 违规原因：seed 模块 index 只做转发。
- 整改建议：`db/seed/index.ts` 直接导入 `./modules/admin/domain`；其他 seed module index 同步删除。

#### [LOW] `db/seed/index.ts:4-15`

- 规范条款：`01-import-boundaries.md` / 禁止跨域导入目录语义路径。
- 问题位置：`./modules/admin`、`./modules/app`、`./modules/forum` 等目录入口。
- 违规原因：依赖 seed 模块 index 转发入口。
- 整改建议：改为直连各模块 `domain.ts`。

### 十、`db/schema`

#### [MEDIUM] `db/schema/app/growth-audit-log.ts:45`

- 规范条款：`05-comments.md` / `smallint` 闭集字段必须写清数值语义。
- 问题位置：`ruleType: smallint()`
- 违规原因：注释只写“取值见成长规则枚举”，未在 schema 字段注释中列出具体数值含义。
- 整改建议：写成 `1=...；2=...` 形式，确保读 schema 即可理解值域。

#### [MEDIUM] `db/schema/app/growth-ledger-record.ts:53`

- 规范条款：同上。
- 问题位置：`ruleType: smallint()`
- 违规原因：同样只引用枚举来源，没有列明值域。
- 整改建议：补齐数值语义。

#### [MEDIUM] `db/schema/app/growth-ledger-record.ts:61`

- 规范条款：同上。
- 问题位置：`targetType: smallint()`
- 违规原因：注释写“取值见对应业务模块的目标类型定义”，要求读者跳转业务代码反推。
- 整改建议：在 schema 注释内写清具体数值含义，或若确为开放值则改为开放业务键说明。

#### [MEDIUM] `db/schema/app/growth-reward-rule.ts:24`

- 规范条款：`05-comments.md` / 闭集状态 / 类型字段 JSDoc 必须写清数值语义。
- 问题位置：`type: smallint().notNull()`
- 违规原因：注释只写“取值见成长规则枚举”，未列出数值。
- 整改建议：补齐 `1=...；2=...` 形式说明。

#### [LOW] `db/schema/app/growth-reward-rule.ts:85-86`

- 规范条款：`07-drizzle.md` / Drizzle 推导类型直接命名为 `XxxSelect`，禁止中间别名。
- 问题位置：`GrowthRewardRule = typeof ...$inferSelect` 后再 `GrowthRewardRuleSelect = GrowthRewardRule`
- 违规原因：存在无意义二次别名链。
- 整改建议：直接写 `export type GrowthRewardRuleSelect = typeof growthRewardRule.$inferSelect`。

#### [LOW] `db/schema/app/growth-rule-usage-counter.ts:68-69`

- 规范条款：同上。
- 问题位置：`GrowthRuleUsageCounter` / `GrowthRuleUsageCounterSelect`
- 违规原因：同类二次别名。
- 整改建议：直接导出 `GrowthRuleUsageCounterSelect = typeof growthRuleUsageCounter.$inferSelect`。

#### [LOW] `db/schema/app/user-asset-balance.ts:57-58`

- 规范条款：同上。
- 问题位置：`UserAssetBalance` / `UserAssetBalanceSelect`
- 违规原因：同类二次别名。
- 整改建议：直接导出 `UserAssetBalanceSelect = typeof userAssetBalance.$inferSelect`。

#### [MEDIUM] `db/schema/app/user-level-rule.ts:86`

- 规范条款：`05-comments.md` / 非枚举 `smallint` 配置字段需写清默认值业务含义。
- 问题位置：`blacklistLimit: smallint().default(10).notNull()`
- 违规原因：注释只写“默认值 10”，没有说明 10 在业务上的含义。
- 整改建议：改为“黑名单上限（10=默认黑名单数量上限）”一类表达。

#### [MEDIUM] `db/schema/app/user-level-rule.ts:90`

- 规范条款：同上。
- 问题位置：`workCollectionLimit: smallint().default(100).notNull()`
- 违规原因：只写默认值，没有写业务语义。
- 整改建议：写清 `100=默认作品收藏上限`。

## 必改项清单

1. 修复 `apps/admin-api/src/app.module.ts` 与 `apps/app-api/src/app.module.ts` 的 `@libs/platform/platform.module` 白名单违规导入。
2. 收敛 `apps/admin-api/src/modules/content/novel/index.ts`、`db/relations/index.ts`、`db/seed/modules/*/index.ts` 等转发入口。
3. 将 `*.types.ts` 历史文件迁移为 `*.type.ts`，优先处理 `platform`、`message`、`moderation`、`admin-api` 下的公共类型文件。
4. 将 service/controller/decorator/dto 文件中的顶层 `type/interface` 移入 owner type 文件。
5. 替换生产代码中的 `any`，优先处理 controller mapper、Swagger options、body compiler / validator。
6. 删除 `libs/platform/src/modules/upload/upload.service.ts:540` 的调试输出。
7. 补齐 `db/schema` 中 `smallint` 字段的数值语义和默认值业务含义注释。
8. 删除 Drizzle inferSelect 的二次别名链。

## 优化建议清单

1. 导入边界建议按“平台白名单 / DB 白名单 / 业务 owner 直连 / 禁止 index 转发”分阶段治理。
2. `05-comments.md` 的“每个方法定义前必须有行注释”与大量装饰器式 controller 代码存在落差，建议明确注释应放在装饰器组之前还是方法定义正上方。
3. DTO 文件中凡是 `type/interface` 且不参与 Swagger class 输出的结构，建议统一迁移到 owner type 文件，避免 DTO 与内部 payload 混杂。
4. schema `smallint` 字段建议形成统一写法：闭集字段写 `1=...；2=...`，非枚举配置字段写清默认值业务含义。
5. 修复规范债时应按模块小步提交，避免一次性重命名大量 type 文件造成导入冲突。

## 结论

Recommendation: COMMENT

当前复核范围内未发现 CRITICAL / HIGH 风险问题；但存在多项 MEDIUM 级规范债，主要集中在导入边界、类型放置、`any`、方法注释和 schema 注释。当前 forum-section 变更链路中的查询条件构造已经符合 Drizzle 禁止嵌套三元的规则，DTO 字段组合和枚举描述也未发现新增违规。
