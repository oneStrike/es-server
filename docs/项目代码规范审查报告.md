# 项目代码规范审查报告

审查日期：2026-04-30  
审查范围：`apps/`、`libs/`、`db/`、`scripts/` 中源码、schema、migration、comments、配置脚本相关文件；本次重点覆盖 `.trae/rules/*` 与根 `AGENTS.md`。  
审查方式：读取根 `AGENTS.md`、`.trae/rules/` 全部 9 份规范、`es-server-code-review` 矩阵，执行 `git diff`、全仓 `rg`/AST 模式扫描、LSP 诊断、类型检查、相关 Jest、schema comments 检查。

Rules checked: 9/9  
Rule points closed: 285/285  
Scope completion: complete（全仓自动扫描完成；方法注释类问题数量很大，按系统性规则点归档并给出代表性位置）

## 审查概览

| 指标              |               数量 / 结果 |
| ----------------- | ------------------------: |
| 规范文档数        |                         9 |
| 规则点总数        |                       285 |
| 已闭合规则点      |                       285 |
| 命中违规的规则点  |                        12 |
| 当前 Git 变更文件 |                         3 |
| 全仓源码扫描文件  |                       975 |
| LSP 诊断          | 2 个变更 TS 文件，0 error |
| 类型检查          |    `pnpm type-check` 通过 |
| 相关测试          |  3 suites / 12 tests 通过 |
| DB comments       |             `Warnings: 0` |

### 风险分布

| 风险等级 | 问题组 | 说明                                                                       |
| -------- | -----: | -------------------------------------------------------------------------- |
| CRITICAL |      0 | 未发现可直接导致凭据泄露、任意执行、数据破坏的确定问题                     |
| HIGH     |      0 | 未发现必须阻断当前格式化变更的高危问题                                     |
| MEDIUM   |      8 | 导入边界、类型放置、方法注释、schema 注释、`any`、调试日志等系统性规范违例 |
| LOW      |      4 | DTO 局部文案常量、Drizzle infer 二次别名、历史转发入口等可分批收敛问题     |

### 当前变更结论

- `libs/forum/src/section/dto/forum-section.dto.ts`：仅格式化导入与 `PickType` 字段换行；未发现新增规范问题。
- `libs/forum/src/section/forum-section.service.ts`：仅格式化换行、缩进与条件表达式；未发现新增行为问题。
- `db/comments/generated.sql`：`git status` 标记修改，但 `git diff -- db/comments/generated.sql` 无内容差异；`pnpm db:comments:check` 通过且 `Warnings: 0`。

## 规范条款逐条校验汇总

| 规范文档                  | 规则点 | 状态   | 违规摘要                                                                            |
| ------------------------- | -----: | ------ | ----------------------------------------------------------------------------------- |
| `PROJECT_RULES.md`        |      4 | 已完成 | 当前变更验证基线已执行；未发现与稳定契约冲突                                        |
| `01-import-boundaries.md` |     31 | 已完成 | 发现平台白名单导入、目录/index 转发入口、`db/relations`/`schema` 相对目录入口问题   |
| `02-controller.md`        |     19 | 已完成 | Controller 路径扫描未发现前导 `/api`、非 `admin/app` 根路径或非 kebab-case 明确命中 |
| `03-dto.md`               |     44 | 已完成 | 发现 DTO 文件内局部 example 常量、DTO 文件内顶层 type/interface 归属不清            |
| `04-typescript-types.md`  |     47 | 已完成 | 发现 `*.types.ts`、非 `*.type.ts` 顶层类型、`any` 使用                              |
| `05-comments.md`          |     42 | 已完成 | 发现大量方法缺少紧邻行注释，部分 `smallint` schema 注释未写清数值语义               |
| `06-error-handling.md`    |     28 | 已完成 | 扫描到 HTTP 异常多为协议/鉴权/校验路径；未确认业务资源失败误用 HTTP 异常的新增问题  |
| `07-drizzle.md`           |     37 | 已完成 | 发现 3 处 Drizzle inferSelect 二次别名；comments 生成检查通过                       |
| `08-testing.md`           |     33 | 已完成 | 当前变更相关 spec 已跑；未发现删除正式 spec 或提交临时探针                          |

## 按文件/模块拆分的详细违规清单

### 1. 导入边界与转发入口

| 等级   | 文件位置                                                                                                                                                                                                               | 对应规范条款                                                      | 违规原因                                                | 整改建议                                                                                    |
| ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- | ------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| MEDIUM | `apps/admin-api/src/app.module.ts:13`                                                                                                                                                                                  | `01-import-boundaries.md`：`libs/platform` 只能使用白名单公共入口 | 使用 `@libs/platform/platform.module`，不在白名单入口中 | 将 `PlatformModule` 暴露到允许的 `@libs/platform/*` 公共入口，调用方走白名单入口            |
| MEDIUM | `apps/app-api/src/app.module.ts:14`                                                                                                                                                                                    | 同上                                                              | 同样直连 `@libs/platform/platform.module`               | 同上                                                                                        |
| LOW    | `apps/admin-api/src/modules/content/novel/index.ts:1`                                                                                                                                                                  | 禁止新增 `index.ts`、禁止目录语义转发入口                         | 文件只做 controller/module 转发                         | 删除转发入口，调用方直连 `novel.controller`、`novel.module` 等 owner 文件                   |
| LOW    | `db/relations/index.ts:1`                                                                                                                                                                                              | `@db/relations` 是明确禁止目录入口示例                            | relations 聚合入口形成被误用风险                        | 仅在 `db/core` 内部保留必要聚合时也应避免对外路径可用，调用方统一 `@db/schema` / `@db/core` |
| LOW    | `db/seed/modules/admin/index.ts:1`、`db/seed/modules/app/index.ts:1`、`db/seed/modules/forum/index.ts:1`、`db/seed/modules/message/index.ts:1`、`db/seed/modules/system/index.ts:1`、`db/seed/modules/work/index.ts:1` | 禁止为了缩短路径新增公共出口文件                                  | seed 模块 index 只转发 `domain.ts`                      | `db/seed/index.ts` 直连各模块 `domain.ts`，删除纯转发 index                                 |
| MEDIUM | `db/core/drizzle.provider.ts:6`、`db/core/drizzle.type.ts:4`                                                                                                                                                           | 禁止 `@db/relations`/目录语义入口                                 | 通过 `../relations` 目录入口聚合 relations              | 改为明确 owner 聚合文件或收敛到 `@db/core` 内部稳定入口，并避免对外可导入目录入口           |
| MEDIUM | `db/comments/schema-comments.ts:7`、`db/core/drizzle.provider.ts:7`、`db/core/drizzle.service.ts:20`、`db/core/drizzle.type.ts:5`、`db/seed/db-client.ts:6`、`db/seed/modules/*/domain.ts`                             | `db` 仅允许 `@db/core`、`@db/extensions`、`@db/schema`            | 多处使用 `../schema` / `../../../schema` 相对目录入口   | 统一通过 `@db/schema` 白名单入口导入                                                        |

### 2. DTO 与类型放置

| 等级   | 文件位置                                                                | 对应规范条款                                      | 违规原因                                                      | 整改建议                                                                  |
| ------ | ----------------------------------------------------------------------- | ------------------------------------------------- | ------------------------------------------------------------- | ------------------------------------------------------------------------- |
| LOW    | `libs/message/src/notification/dto/notification-unread.dto.ts:3`、`:16` | DTO 短文本元信息默认就地内联，不因局部复用抽常量  | `NOTIFICATION_UNREAD_*_EXAMPLE` 只服务当前 DTO 文件的 example | 若无跨文件事实源需求，内联到装饰器；若确需共享，补充跨文件使用依据        |
| MEDIUM | `apps/admin-api/src/common/decorators/api-audit-doc.decorator.ts:8`     | 纯 TypeScript type/interface 统一放在 `*.type.ts` | decorator 文件内直接导出 interface                            | 移入同域 `api-audit-doc.type.ts` 或已有 owner type 文件                   |
| MEDIUM | `libs/config/src/system-config/config-reader.ts:7`                      | 同上                                              | reader 文件内导出 `SystemConfig` type                         | 移入 `system-config.type.ts`，reader 仅保留运行时逻辑                     |
| MEDIUM | `libs/growth/src/growth-ledger/growth-balance-query.service.ts:6`       | 同上                                              | service 文件内导出 `UserGrowthSnapshot` interface             | 移入 `growth-ledger.type.ts` 或同域 `types/*.type.ts`                     |
| MEDIUM | `libs/message/src/inbox/inbox.service.ts:11`                            | 同上                                              | service 文件内导出 `InboxLatestChatSummary` interface         | 移入 `inbox.type.ts`                                                      |
| MEDIUM | `libs/message/src/notification/dto/notification.dto.ts:321` 等          | DTO 文件禁止承载非 HTTP 内部 type/interface       | DTO 文件内混入多组 `type` / `interface` 数据结构              | 若服务 Swagger 文档，改为 DTO class；若是内部结构，移入 owner `*.type.ts` |
| MEDIUM | `libs/platform/src/config/db.config.ts:4`、`:6`、`:8`、`:14`            | 纯类型定义统一放入 `*.type.ts`                    | config 文件内承载可复用类型                                   | 拆至 `db-config.type.ts`                                                  |

全仓同类扫描结果：非 `*.type.ts` / 非 `*.types.ts` / 非 `db/schema` 文件中，顶层导出 `type/interface` 共 95 处；需按 owner 模块批量收敛。

### 3. `*.types.ts` 与目录级类型入口

| 等级   | 文件位置                                                                                                                       | 对应规范条款                                          | 违规原因                        | 整改建议                                    |
| ------ | ------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------- | ------------------------------- | ------------------------------------------- |
| MEDIUM | `apps/admin-api/src/common/decorators/audit.types.ts:1`                                                                        | 禁止新增 `*.types.ts`，历史文件默认收敛到 `*.type.ts` | 文件命名不符合 `*.type.ts` 约定 | 重命名为 `audit.type.ts` 并更新直连导入     |
| MEDIUM | `libs/message/src/notification/notification-websocket.types.ts:1`                                                              | 同上                                                  | 同上                            | 重命名为 `notification-websocket.type.ts`   |
| MEDIUM | `libs/moderation/sensitive-word/src/sensitive-word.types.ts:1`                                                                 | 同上                                                  | 同上                            | 重命名为 `sensitive-word.type.ts`           |
| MEDIUM | `libs/platform/src/config/aliyun.types.ts:1`、`upload.types.ts:1`                                                              | 同上                                                  | 同上                            | 重命名为 `aliyun.type.ts`、`upload.type.ts` |
| MEDIUM | `libs/platform/src/modules/auth/auth.types.ts:1`、`login-guard.types.ts:1`、`token-storage.types.ts:1`                         | 同上                                                  | 同上                            | 迁移到 `*.type.ts` 或模块 `types/*.type.ts` |
| MEDIUM | `libs/platform/src/modules/captcha/captcha.types.ts:1`、`geo/geo.types.ts:1`、`sms/sms.types.ts:1`、`upload/upload.types.ts:1` | 同上                                                  | 同上                            | 同上                                        |
| MEDIUM | `libs/platform/src/utils/bytes.types.ts:1`、`request-parse.types.ts:1`、`libs/platform/src/platform.module.types.ts:1`         | 同上                                                  | 同上                            | 同上                                        |

### 4. 边界类型与 `any`

| 等级   | 文件位置                                                         | 对应规范条款                             | 违规原因                                          | 整改建议                                                                        |
| ------ | ---------------------------------------------------------------- | ---------------------------------------- | ------------------------------------------------- | ------------------------------------------------------------------------------- |
| MEDIUM | `apps/admin-api/src/modules/forum/topic/topic.controller.ts:52`  | 禁止使用 `any`                           | `Record<string, any>` 作为 controller mapper 输入 | 从实际 service 返回 DTO/type 推导，替换为命名类型                               |
| MEDIUM | `libs/platform/src/decorators/api-doc.decorator.ts:20`           | 禁止使用 `any`                           | `Record<string, any>` 扩大 Swagger 模型配置边界   | 使用 `Record<string, unknown>` 并在消费点收窄                                   |
| MEDIUM | `libs/interaction/src/body/body-compiler.service.ts:159`、`:183` | 使用 `unknown` 后必须收窄，禁止 `as any` | 错误信息读取时用 `(node as any).type`             | 为 block/inline union 增加 exhaustiveness helper，或用 `never` 分支保留类型安全 |
| MEDIUM | `libs/interaction/src/body/body-validator.service.ts:274`        | 禁止 `any`                               | `Record<string, any>` 类型守卫                    | 改为 `Record<string, unknown>`，消费前逐项校验                                  |
| MEDIUM | `db/migrate.ts:117`、`:121`                                      | 禁止 `any`                               | 迁移脚本错误 cause 链使用 `(error as any)`        | 定义错误 cause 结构守卫或使用 `unknown` 逐步收窄                                |

测试文件中的 `as any` / `tx: any` 也有命中，但风险低于生产路径，建议在对应测试维护时一并替换为局部测试接口。

### 5. 注释规范

| 等级   | 文件位置                                                                               | 对应规范条款                                   | 违规原因                                                | 整改建议                                                                                                     |
| ------ | -------------------------------------------------------------------------------------- | ---------------------------------------------- | ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| MEDIUM | `apps/admin-api/src/modules/admin-user/admin-user.controller.ts:38` 等 controller 方法 | 每个方法定义前必须有紧邻行注释                 | 大量 controller 方法仅有装饰器，无方法注释              | 在方法定义正上方补 1-2 行中文行注释；如需保留装饰器，注释应贴近方法定义或统一团队例外                        |
| MEDIUM | `apps/admin-api/src/modules/auth/auth.service.ts:39`、`:43` 等 getter                  | 同上                                           | getter 方法无说明                                       | 给非显而易见 getter 补简短语义注释；若 getter 只是噪声，可评估是否规则例外                                   |
| MEDIUM | `libs/forum/src/section/forum-section.service.ts:39`、`:43`、`:47`                     | 同上                                           | 当前变更文件中的 getter 未写方法注释                    | 补充“返回 Drizzle db / forumSection table / forumSectionGroup table”等简短行注释，或在规范中明确 getter 豁免 |
| MEDIUM | 全仓方法注释扫描                                                                       | 同上                                           | 自动初筛共 1234 个方法定义缺少紧邻注释                  | 按模块分批补齐，优先 service/helper/复杂业务方法，避免模板化废话                                             |
| MEDIUM | `db/schema/app/growth-audit-log.ts:45`                                                 | `smallint` 闭集字段必须写清数值语义            | `ruleType` 注释只写“取值见成长规则枚举”                 | 写成 `1=...；2=...` 等中文值域说明                                                                           |
| MEDIUM | `db/schema/app/growth-ledger-record.ts:53`、`:61`                                      | 同上                                           | `ruleType`、`targetType` 未在 schema 注释中给出数值语义 | 补齐数值值域或说明开放键不适用                                                                               |
| MEDIUM | `db/schema/app/growth-reward-rule.ts:24`                                               | 同上                                           | `type` 注释未列出具体数字值域                           | 补齐 `1=...` 形式说明                                                                                        |
| MEDIUM | `db/schema/app/growth-reward-settlement.ts:40`                                         | 同上                                           | `targetType` 注释过于空泛                               | 补齐值域或改为开放业务键说明                                                                                 |
| MEDIUM | `db/schema/app/user-level-rule.ts:86`、`:90`                                           | 非枚举 `smallint` 配置字段需写清默认值业务含义 | 只写“默认值 10/100”，未说明业务含义                     | 写明 `10=默认黑名单上限`、`100=默认收藏上限`                                                                 |
| MEDIUM | `db/schema/forum/forum-hashtag-reference.ts:54`                                        | 非枚举 `smallint` 配置字段需写清默认值业务含义 | `occurrenceCount` 默认 1 未解释业务含义                 | 写明 `1=单次出现` 等默认语义                                                                                 |

### 6. Drizzle 与 schema 联动

| 等级 | 文件位置                                           | 对应规范条款                                                           | 违规原因                                                                                     | 整改建议                                                                                       |
| ---- | -------------------------------------------------- | ---------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| LOW  | `db/schema/app/growth-reward-rule.ts:85-86`        | Drizzle infer 类型应直接命名为 `XxxSelect` / `XxxInsert`，禁止二次别名 | `GrowthRewardRule = typeof ...$inferSelect` 后再 `GrowthRewardRuleSelect = GrowthRewardRule` | 删除中间别名，直接 `export type GrowthRewardRuleSelect = typeof growthRewardRule.$inferSelect` |
| LOW  | `db/schema/app/growth-rule-usage-counter.ts:68-69` | 同上                                                                   | 同上                                                                                         | 同上                                                                                           |
| LOW  | `db/schema/app/user-asset-balance.ts:57-58`        | 同上                                                                   | 同上                                                                                         | 同上                                                                                           |

本次 `db/comments/generated.sql` 内容 diff 为空，`pnpm db:comments:check` 输出 `Warnings: 0`，schema comments 生成链路当前未发现失配。

### 7. 安全、调试输出与工程风格

| 等级   | 文件位置                                                 | 对应规范条款                                  | 违规原因                                           | 整改建议                                                   |
| ------ | -------------------------------------------------------- | --------------------------------------------- | -------------------------------------------------- | ---------------------------------------------------------- |
| MEDIUM | `libs/platform/src/modules/upload/upload.service.ts:540` | 工程风格 / 日志与诊断应结构化，不提交临时日志 | 生产 service 中存在 `console.log(normalizedScene)` | 删除临时日志；如确需诊断，使用统一 Logger 并带结构化上下文 |

`console.log` 在 `scripts/db-comments.ts`、`db/seed/**`、`db/migrate.ts`、`bootstrap/logStartupInfo.ts` 中属于 CLI/启动输出场景，本次不作为生产业务代码违规。

## 整体合规率总结

- 规则点合规率：`273 / 285 = 95.79%`（按规则点维度；存在违规的规则点计为不合规）。
- 当前变更合规率：`100%`（当前两个 TS diff 均为格式化，相关 LSP/type-check/test 通过）。
- 全仓实例合规风险：中等。主要不是当前 diff 引入，而是历史全仓规范债集中在导入边界、类型文件命名、方法注释和 schema 注释。

## 必改项清单

1. 修复 `@libs/platform/platform.module` 两处白名单违规导入，避免平台入口继续漂移。
2. 收敛 `db/relations/index.ts`、`apps/.../novel/index.ts`、`db/seed/modules/*/index.ts` 等转发入口。
3. 迁移 15 个 `*.types.ts` 文件到 `*.type.ts` 或 owner `types/*.type.ts`。
4. 处理生产路径 `any`：优先 `topic.controller.ts`、`api-doc.decorator.ts`、`body-*`、`db/migrate.ts`。
5. 补齐 `db/schema` 中 8 个 `smallint` 字段的数值/默认值业务语义注释。
6. 删除 `libs/platform/src/modules/upload/upload.service.ts:540` 的临时 `console.log`。

## 优化建议清单

1. 建议新增 import-boundary 静态检查脚本，把平台/DB 白名单、业务 owner 直连、禁止 `index.ts` 转发纳入 CI。
2. 建议新增 `*.types.ts` / 非 `*.type.ts` 顶层类型扫描，先以 warning 运行，完成批量迁移后升级为 error。
3. 方法注释规则当前与大量 controller 装饰器代码存在冲突，建议明确“注释应位于装饰器之前还是方法定义紧邻上一行”，再分模块整改。
4. 对 schema `smallint` 注释建议新增脚本检查 `smallint` 字段附近是否包含 `1=...` 或默认值语义，避免 comments 文档再漂移。
5. 当前 diff 没有新增测试需求；后续若修改导入边界、schema 注释或类型文件迁移，按模块运行对应 spec 加 `pnpm type-check`。

## 验证记录

- `git diff --stat`：`libs/forum/src/section/dto/forum-section.dto.ts`、`libs/forum/src/section/forum-section.service.ts` 有格式化 diff；`db/comments/generated.sql` 仅工作区标记，无内容 diff。
- `lsp_diagnostics`：
  - `libs/forum/src/section/dto/forum-section.dto.ts`：0 diagnostics
  - `libs/forum/src/section/forum-section.service.ts`：0 diagnostics
- `pnpm type-check`：通过。
- `pnpm test -- --runInBand --runTestsByPath libs/forum/src/section/forum-section.service.spec.ts libs/forum/src/section/dto/forum-section.dto.spec.ts apps/app-api/src/modules/forum/forum-section.controller.spec.ts`：3 suites / 12 tests 通过。
- `pnpm db:comments:check`：`Warnings: 0`。

## Verdict

COMMENT。当前待审 diff 本身未发现必须退回的问题；但全仓规范专项审查发现多项历史规范债，建议作为独立整改任务分批处理。
