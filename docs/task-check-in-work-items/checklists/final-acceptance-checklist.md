# Task / Check-In 最终验收清单

本清单只记录验收结论与证据位，不反向定义新范围。任务排序、依赖与状态以 [execution-plan.md](/D:/code/es/es-server/docs/task-check-in-work-items/execution-plan.md) 为准。

## 一期边界与治理验收

| 验收项 | 状态 | 证据 |
| --- | --- | --- |
| `GrowthRuleTypeEnum.DAILY_CHECK_IN = 6` 已保留为稳定预留编码 | `[ ]` | 待补代码引用 |
| `DAILY_CHECK_IN` 已不出现在当前可配置事件视图 | `[ ]` | 待补接口响应 / 单测结果 |
| seed 已不再预置签到积分/经验规则 | `[ ]` | 待补 seed 变更与脚本结果 |
| 一期签到方案文档已明确“不依赖 task、不依赖现存签到事件配置” | `[ ]` | 待补文档链接 |
| 关键日期字段已按 `date` 语义建模，未用 timestamp 伪装自然日 | `[ ]` | 待补 schema / DTO / SQL 结果 |
| 计划版本与周期/奖励快照已冻结历史事实，不受后续配置变更影响 | `[ ]` | 待补 schema 变更 / 单测结果 |
| 成长账本来源已补齐 `check_in_base_bonus`、`check_in_streak_bonus` | `[ ]` | 待补枚举引用 / 接口结果 |

## 功能验收

| 验收项 | 状态 | 证据 |
| --- | --- | --- |
| 用户可完成今日签到，且同一天重复提交保持幂等 | `[ ]` | 待补接口响应 / 单测结果 |
| 用户可在当前周期内对历史漏签日补签 | `[ ]` | 待补接口响应 / SQL 结果 |
| 用户不能补未来日期、跨周期日期或超出补签上限的日期 | `[ ]` | 待补失败用例结果 |
| 用户跨多个周期未参与时不会补建历史空周期，重新进入后只运行当前周期 | `[ ]` | 待补接口响应 / SQL 结果 |
| 用户可查看签到摘要、签到日历与签到记录 | `[ ]` | 待补接口响应 / 页面截图 |
| 管理端可配置计划、基础奖励、连续奖励和周期参数 | `[ ]` | 待补接口响应 / 页面截图 |
| `daily` / `weekly` / `monthly` 周期边界与 `cycleKey` 生成规则符合约定 | `[ ]` | 待补周期测试结果 |

## 奖励与结算验收

| 验收项 | 状态 | 证据 |
| --- | --- | --- |
| 基础签到奖励直接由签到域进入成长账本 | `[ ]` | 待补账本记录 / 日志 |
| 未配置基础签到奖励的计划不会生成伪造的待发奖励状态，也不会进入补偿队列 | `[ ]` | 待补接口响应 / SQL 结果 |
| 连续签到达到阈值后可稳定发放连续奖励 | `[ ]` | 待补奖励发放记录 / 账本记录 |
| 连续奖励不会重复发放，除非规则显式允许重复 | `[ ]` | 待补单测结果 / SQL 结果 |
| 奖励失败时不会回滚签到事实，且可通过补偿恢复 | `[ ]` | 待补补偿脚本 / 日志 |
| 补签在规则允许时可补发首次满足条件的连续奖励 | `[ ]` | 待补补签触发测试 / SQL 结果 |
| 同次补签/补算命中多个历史未发阈值时会一次性补发全部应发奖励 | `[ ]` | 待补补算测试 / SQL 结果 |
| 未命中连续阈值时不会生成伪造的 `check_in_streak_reward_grant` 待发记录 | `[ ]` | 待补 SQL 结果 / 单测结果 |
| 账本与审计上下文可追溯 `planId` `cycleId` `recordId` `grantId` | `[ ]` | 待补账本记录 / 日志 |

## 回归与稳定性验收

| 验收项 | 状态 | 证据 |
| --- | --- | --- |
| `task` 现有任务查询、认领、完成链路未受影响 | `[ ]` | 待补回归测试结果 |
| Growth 现有非签到规则配置能力未受影响 | `[ ]` | 待补接口响应 / 回归测试 |
| 并发签到、重复补签和补偿重试都满足幂等约束 | `[ ]` | 待补压测或并发测试结果 |
| 对账视图能识别缺账、漏发、重复发放等异常 | `[ ]` | 待补对账脚本 / 查询结果 |
| `check_in_cycle` / `check_in_record` / `check_in_streak_reward_grant` 的唯一索引已覆盖主链路幂等场景 | `[ ]` | 待补 schema / 并发测试结果 |

## 阻塞上线项

| 项目 | 是否阻塞 | 说明 | 解除条件 |
| --- | --- | --- | --- |
| 签到主链路并发幂等未验证 | `[ ]` | 若未验证，可能出现重复签到或重复落账 | 提供并发测试结果 |
| 奖励补偿入口未验证 | `[ ]` | 若补偿不可用，奖励异常无法修复 | 提供补偿演练结果 |
| 管理端配置和用户端读模型未联调 | `[ ]` | 若未联调，配置可能无法正确投射到用户视图 | 提供联调记录 |
| 历史配置冻结未验证 | `[ ]` | 若未验证，计划更新后可能污染已完成周期和奖励事实 | 提供版本快照测试结果 |

## 最终签收问题与结论

| 项目 | 记录 |
| --- | --- |
| 最终遗留问题 | 待补 |
| 豁免说明 | 待补 |
| 最终签收结论 | 待补 |
| 签收人 / 日期 | 待补 |
