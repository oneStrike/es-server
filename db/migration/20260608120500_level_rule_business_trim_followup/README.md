# Level Rule Business Trim Follow-up

Normalizes padded nonblank `user_level_rule.business` values after checking that the normalized enabled threshold set remains unique.

This keeps persisted business domains aligned with runtime `trim()` normalization.
