interface:
  display_name: 'ES Server 规范审查'
  short_description: '中文输出，逐条闭合 .trae/rules 全部规范点'
  default_prompt: '使用 $es-server-code-review 审查这个 es-server 变更，先完整重读适用 AGENTS.md 与 .trae/rules，再把每条规范点展开成矩阵，逐条检查并仅输出规范问题与覆盖情况，直到所有规则点闭合后再结束。'
