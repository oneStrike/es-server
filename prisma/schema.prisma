generator client {
  provider        = "prisma-client"
  output          = "./prismaClient"
  engineType      = "client"
  previewFeatures = ["relationJoins"]
}

datasource db {
  provider = "postgresql"
}
